#pragma once

#include <iostream>
#include <string>
#include <vector>

using namespace std;

class Tuple : public vector<string>
{
  /*
public:
  bool isjoinable(Schema scheme1, Tuple tup1, Schema scheme2, Tuple tup2);
  */
};
//wrapper classes to make more readable
